# Normalize line endings: LF in the repo, native on checkout for text files.
* text=auto eol=lf

# Explicit binary-safe / generated markers
*.pod    text
*.m4     text
Changes  text

# Keep the project counted as C on GitHub (don't let the bundled wepoll skew stats).
*.c linguist-language=C
*.h linguist-language=C
